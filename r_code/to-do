To-do

1. Grafiken mit ggplot
2. Tabellen mit pixydust

# Bei punkt 1. sollten der bestehende code zu den base plots ersetzt werden (die ggplots solten also equivalent zu den base plots sein),
  sodass die scripts 'ortho_poly_contrasts_ggplot.r' und 'plotting_functions.r' gelöscht werden können
  
# Bei punkt 1. sollten iwie die plots bei jeder funktion auch asgegeben werden koennen (moeglicherweise als funktionsbefehl z.B. - pdf = TRUE -)
  sodass bei der summarisation function nicht immer nur die letzte erstellte grafik angezeigt wird sondern alle graficen ausgegeben werden

# Bei punkt 2. wäre mein vorschlag, dass die umwandlung der dataframes in pixydust-tabellen moglichst erst in der summariation function passiert,
  da alle anderen funktionen jetzt mit den data.frame ausgaben bzw. list ausgaben arbeiten. Vlt gibt es auch andere alternativen 
  (kenne mich zu wenig mit pixydust aus)
  
DONE <- 3. Data preparation braucht den listenweisen fallausschluss (listwise deletion auch in Datasimulation eingeführt)
4. Data simulation nach empirischem "vorbild" 

# Data simulation und data preparation sollten wir vlt in ein einzelnes r-script schreiben

# Bei punkt 4. bietet sich der ringelmann effekt (z.B. kravitz & martin, 1986) oder alternativ soziales faulenzen (z.B. latané, 1979) an
  (siehe whatsapp posts von fredi und mir).
  Jedenfalls kann man beide effekte in dem empirischen Teil näher beschreiben eventuell auch den Unterschied falls wir zu wenig stoff haben.
  
5. Unsere kommentare entfernen sowie ungenutzten code 
   (außer in 'q3' in der erstellung der helmert matrix ist auskommentierter code der bleiben soll).
   Ich habe mir mühe gegeben solchen code mit zwei rauten auszukommentieren '##'
  
6. Style-guide check!!!